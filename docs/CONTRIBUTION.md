# Contribution Notes

The full frontend contribution guide lives at [`../CONTRIBUTING.md`](../CONTRIBUTING.md).

Use that guide for pull request workflow, naming standards, validation commands, styling rules, and
review expectations. In particular, check `src/config/colors.ts` before adding new color values so
the UI stays consistent across the project.
